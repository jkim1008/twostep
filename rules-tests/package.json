{
  "name": "rules-tests",
  "private": true,
  "description": "Firestore security-rules test suite — runs against the emulator",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase-admin": "^13.0.0",
    "vitest": "^3.0.0"
  }
}
